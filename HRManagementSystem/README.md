# نظام إدارة الموارد البشرية
# HR Management System

<div align="center">

![.NET](https://img.shields.io/badge/.NET-8.0-512BD4?style=for-the-badge&logo=dotnet)
![WPF](https://img.shields.io/badge/WPF-Desktop-blue?style=for-the-badge)
![SQLite](https://img.shields.io/badge/SQLite-Database-003B57?style=for-the-badge&logo=sqlite)
![Arabic](https://img.shields.io/badge/RTL-Arabic-green?style=for-the-badge)
![License](https://img.shields.io/badge/License-MIT-yellow?style=for-the-badge)

**نظام متكامل لإدارة الموارد البشرية مبني بتقنية C# .NET 8 مع واجهة مستخدم عربية RTL**

[التثبيت](#-التثبيت) • [التشغيل](#-التشغيل) • [الميزات](#-الميزات) • [لقطات الشاشة](#-لقطات-الشاشة)

</div>

---

## 📋 المحتويات

- [المتطلبات](#-المتطلبات)
- [التثبيت](#-التثبيت)
- [التشغيل](#-التشغيل)
- [دليل الاستخدام](#-دليل-الاستخدام)
- [الميزات](#-الميزات)
- [البناء والنشر](#-البناء-والنشر)
- [هيكل المشروع](#-هيكل-المشروع)
- [قاعدة البيانات](#-قاعدة-البيانات)
- [الإعدادات](#-الإعدادات)
- [إضافة وحدات جديدة](#-إضافة-وحدات-جديدة)

---

## 🔧 المتطلبات

### متطلبات التطوير
| المتطلب | الإصدار | الرابط |
|---------|---------|--------|
| .NET SDK | 8.0+ | [تحميل](https://dotnet.microsoft.com/download/dotnet/8.0) |
| Visual Studio | 2022+ | [تحميل](https://visualstudio.microsoft.com/) |
| Git | أحدث إصدار | [تحميل](https://git-scm.com/) |

### متطلبات التشغيل
- **نظام التشغيل:** Windows 10/11 (64-bit)
- **.NET Runtime:** مضمن في النسخة المستقلة (Self-Contained)

---

## 📥 التثبيت

### الطريقة 1: تشغيل النسخة الجاهزة (Portable)

1. **حمّل أحدث إصدار** من [Releases](https://github.com/aliahmad1967/HR-SQLite-management/releases)
2. **فك الضغط** عن الملف
3. **شغّل** `HRManagementSystem.exe`

### الطريقة 2: البناء من المصدر

```powershell
# 1. استنساخ المشروع
git clone https://github.com/aliahmad1967/HR-SQLite-management.git
cd HR-SQLite-management/HRManagementSystem

# 2. استعادة الحزم
dotnet restore

# 3. البناء
dotnet build -c Release

# 4. النشر (ملف تنفيذي واحد)
dotnet publish src/HRManagementSystem/HRManagementSystem.csproj -c Release -r win-x64 --self-contained -p:PublishSingleFile=true -o ./publish
```

---

## 🚀 التشغيل

### تشغيل مباشر (للتطوير)
```powershell
cd HRManagementSystem
dotnet run --project src/HRManagementSystem/HRManagementSystem.csproj
```

### تشغيل النسخة المنشورة
```powershell
.\publish\HRManagementSystem.exe
```

### 🔐 بيانات تسجيل الدخول الافتراضية

| الحقل | القيمة |
|-------|--------|
| **اسم المستخدم** | `admin` |
| **كلمة المرور** | `admin123` |

> ⚠️ **تنبيه:** يُنصح بتغيير كلمة المرور فوراً بعد أول تسجيل دخول

---

## 📖 دليل الاستخدام

### 1. تسجيل الدخول
1. افتح التطبيق
2. أدخل اسم المستخدم: `admin`
3. أدخل كلمة المرور: `admin123`
4. اضغط "دخول"

### 2. لوحة التحكم
- عرض إجمالي الموظفين والأقسام
- إحصائيات الحضور اليومي
- الإجازات المعلقة
- إجمالي الرواتب الشهرية

### 3. إدارة الموظفين
1. اذهب إلى **"الموظفين"** من القائمة الجانبية
2. اضغط **"إضافة موظف"** لإضافة موظف جديد
3. املأ البيانات المطلوبة:
   - الرقم الوظيفي
   - الاسم الكامل
   - القسم والمنصب
   - تاريخ التعيين
   - الراتب الأساسي
4. يمكنك إضافة **صورة للموظف**
5. اضغط **"حفظ"**

### 4. إدارة الحضور
1. اذهب إلى **"الحضور"**
2. اختر التاريخ
3. سجّل حضور/انصراف الموظفين
4. يمكنك عرض تقارير الحضور الشهرية

### 5. إدارة الإجازات
1. اذهب إلى **"الإجازات"**
2. اضغط **"طلب إجازة جديد"**
3. اختر الموظف ونوع الإجازة
4. حدد تاريخ البداية والنهاية
5. يمكن للمدير الموافقة أو الرفض

### 6. الرواتب
1. اذهب إلى **"الرواتب"**
2. اختر **السنة والشهر**
3. اضغط **"توليد رواتب الشهر"**
4. ستظهر رواتب جميع الموظفين
5. يمكنك:
   - 🧮 **حساب** - حساب تفاصيل الراتب
   - ✓ **اعتماد** - اعتماد الراتب
   - 💵 **صرف** - تسجيل صرف الراتب
   - 🖨️ **طباعة** - طباعة كشف الراتب

### 7. الإعدادات
1. اذهب إلى **"الإعدادات"**
2. **الوضع الداكن/الفاتح:** تبديل مظهر التطبيق
3. **النسخ الاحتياطي:** إنشاء واستعادة نسخ احتياطية

---

## ✨ الميزات

### 🏢 إدارة الموظفين
- ✅ إضافة/تعديل/حذف الموظفين
- ✅ صور الموظفين
- ✅ البحث والفلترة
- ✅ ربط بالأقسام والمناصب
- ✅ تتبع حالة التوظيف

### ⏰ الحضور والانصراف
- ✅ تسجيل الحضور والانصراف
- ✅ حساب ساعات العمل
- ✅ تتبع العمل الإضافي
- ✅ تقارير الحضور

### 🏖️ إدارة الإجازات
- ✅ أنواع إجازات متعددة (سنوية، مرضية، بدون راتب، زواج، وفاة)
- ✅ طلب وموافقة الإجازات
- ✅ تتبع أرصدة الإجازات

### 💰 الرواتب
- ✅ توليد الرواتب الشهرية
- ✅ حساب البدلات والخصومات
- ✅ اعتماد وصرف الرواتب
- ✅ طباعة كشوف الرواتب
- ✅ العملة: الدينار العراقي (د.ع)

### 🏗️ الأقسام والمناصب
- ✅ إدارة الهيكل التنظيمي
- ✅ تعيين مدراء الأقسام

### 📊 لوحة التحكم
- ✅ إحصائيات حية من قاعدة البيانات
- ✅ عدد الموظفين والأقسام
- ✅ الحضور اليومي
- ✅ الإجازات المعلقة

### ⚙️ الإعدادات
- ✅ الوضع الداكن/الفاتح
- ✅ النسخ الاحتياطي والاستعادة
- ✅ حفظ تفضيلات المستخدم

---

## 📦 البناء والنشر

### بناء للتطوير
```powershell
cd HRManagementSystem
dotnet build -c Debug
```

### بناء للإنتاج (ملف تنفيذي واحد)
```powershell
dotnet publish src/HRManagementSystem/HRManagementSystem.csproj `
    -c Release -r win-x64 --self-contained `
    -p:PublishSingleFile=true `
    -p:IncludeNativeLibrariesForSelfExtract=true `
    -o ./publish
```

### مخرجات البناء
```
publish/
├── HRManagementSystem.exe    # الملف التنفيذي الرئيسي
├── Data/
│   ├── hr_database.db        # قاعدة البيانات (تُنشأ تلقائياً)
│   └── Images/               # صور الموظفين
├── Backups/                  # النسخ الاحتياطية
└── logs/                     # سجلات التطبيق
```

---

## 📁 هيكل المشروع

```
HRManagementSystem/
├── src/
│   ├── HRManagementSystem/           # 🖥️ تطبيق WPF الرئيسي
│   │   ├── Views/                    # شاشات XAML
│   │   ├── ViewModels/               # ViewModels (MVVM)
│   │   ├── Converters/               # محولات القيم
│   │   └── Properties/               # إعدادات المستخدم
│   │
│   ├── HRManagementSystem.Core/      # 📦 النماذج والواجهات
│   │   ├── Models/                   # كيانات البيانات
│   │   ├── Enums/                    # التعدادات
│   │   └── Interfaces/               # واجهات المستودعات
│   │
│   ├── HRManagementSystem.Data/      # 🗄️ طبقة البيانات
│   │   ├── Database/                 # سياق قاعدة البيانات
│   │   └── Repositories/             # تنفيذ المستودعات
│   │
│   └── HRManagementSystem.Services/  # ⚙️ طبقة الخدمات
│       └── Interfaces/               # واجهات الخدمات
│
├── Database/Schema/                  # 📋 ملفات SQL
├── scripts/                          # 🔧 سكريبتات البناء
├── publish/                          # 📤 مخرجات النشر
└── README.md                         # 📖 هذا الملف
```

---

## 🗄️ قاعدة البيانات

### التقنية المستخدمة
- **SQLite** - قاعدة بيانات خفيفة ومحمولة
- **Dapper** - ORM سريع وفعال
- **إنشاء تلقائي** - تُنشأ قاعدة البيانات والجداول تلقائياً عند أول تشغيل

### هيكل الجداول الرئيسية

| الجدول | الوصف |
|--------|-------|
| `Departments` | الأقسام |
| `Positions` | المناصب الوظيفية |
| `Employees` | الموظفين |
| `Users` | مستخدمي النظام |
| `Attendance` | سجلات الحضور |
| `LeaveTypes` | أنواع الإجازات |
| `Leaves` | طلبات الإجازات |
| `Payroll` | الرواتب الشهرية |

### النسخ الاحتياطي

**من داخل التطبيق:**
1. اذهب إلى **الإعدادات**
2. اضغط **"إنشاء نسخة احتياطية"**

**يدوياً:**
```powershell
# إنشاء نسخة احتياطية
Copy-Item "publish/Data/hr_database.db" "publish/Backups/backup_$(Get-Date -Format 'yyyyMMdd_HHmmss').db"

# استعادة نسخة احتياطية
Copy-Item "publish/Backups/backup_20241201_120000.db" "publish/Data/hr_database.db"
```

---

## ⚙️ الإعدادات

### ملفات الإعدادات

| الملف | الموقع | الوصف |
|-------|--------|-------|
| `user.config` | `%AppData%/HRManagementSystem` | تفضيلات المستخدم |
| `hr_database.db` | `Data/` | قاعدة البيانات |

### الإعدادات المحفوظة
- **الوضع الداكن/الفاتح** - يُحفظ تلقائياً
- **آخر مستخدم** - يُحفظ لتسهيل الدخول

---

## 🔐 الأمان

| الميزة | الوصف |
|--------|-------|
| 🔒 تشفير كلمات المرور | BCrypt مع Salt تلقائي |
| 🚫 قفل الحساب | بعد 5 محاولات فاشلة |
| 📝 سجل التدقيق | تسجيل جميع العمليات |
| 👥 صلاحيات المستخدمين | Admin, HRManager, Viewer |

---

## 🛠️ التقنيات المستخدمة

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| .NET | 8.0 | إطار العمل الأساسي |
| WPF | - | واجهة المستخدم |
| SQLite | 3.x | قاعدة البيانات |
| Dapper | 2.x | ORM |
| Material Design | 5.x | تصميم الواجهة |
| CommunityToolkit.Mvvm | 8.x | نمط MVVM |
| BCrypt.Net-Next | 4.x | تشفير كلمات المرور |
| Serilog | 3.x | تسجيل الأحداث |

---

## 🔌 إضافة وحدات جديدة

### 1. إنشاء النموذج (Model)
```csharp
// src/HRManagementSystem.Core/Models/NewEntity.cs
public class NewEntity : BaseEntity
{
    public string Name { get; set; } = string.Empty;
}
```

### 2. إنشاء المستودع (Repository)
```csharp
// src/HRManagementSystem.Data/Repositories/NewEntityRepository.cs
public class NewEntityRepository : BaseRepository<NewEntity>
{
    public NewEntityRepository(IDatabaseContext context)
        : base(context, "NewEntities") { }
}
```

### 3. إنشاء الخدمة (Service)
```csharp
// src/HRManagementSystem.Services/NewEntityService.cs
public class NewEntityService : INewEntityService
{
    private readonly IUnitOfWork _unitOfWork;
    public NewEntityService(IUnitOfWork unitOfWork) => _unitOfWork = unitOfWork;
}
```

### 4. إنشاء ViewModel
```csharp
// src/HRManagementSystem/ViewModels/NewEntityViewModel.cs
public partial class NewEntityViewModel : BaseViewModel
{
    [ObservableProperty]
    private ObservableCollection<NewEntity> _items = new();
}
```

### 5. تسجيل في DI (App.xaml.cs)
```csharp
services.AddScoped<INewEntityService, NewEntityService>();
services.AddTransient<NewEntityViewModel>();
```

---

## 📞 الدعم والمساهمة

### الإبلاغ عن مشاكل
1. افتح [Issue جديد](https://github.com/aliahmad1967/HR-SQLite-management/issues)
2. اشرح المشكلة بالتفصيل
3. أرفق لقطات شاشة إن أمكن

### المساهمة
1. Fork المشروع
2. أنشئ Branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى Branch (`git push origin feature/amazing-feature`)
5. افتح Pull Request

---

## 👨‍💻 المطور

**Ali Ahmad**
- GitHub: [@aliahmad1967](https://github.com/aliahmad1967)

---

## 📄 الترخيص

هذا المشروع مرخص تحت **MIT License** - راجع ملف [LICENSE](LICENSE.txt) للتفاصيل.

---

<div align="center">

**صُنع بـ ❤️ في العراق 🇮🇶**

</div>
