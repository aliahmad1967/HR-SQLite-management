<!-- =====================================================
     نظام إدارة الموارد البشرية - HR Management System
     App.xaml - ملف التطبيق الرئيسي
     ===================================================== -->
<Application x:Class="HRManagementSystem.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:HRManagementSystem.Converters"
             xmlns:viewmodels="clr-namespace:HRManagementSystem.ViewModels"
             xmlns:views="clr-namespace:HRManagementSystem.Views">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Material Design -->
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="Blue" SecondaryColor="Teal" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- المحولات - Converters -->
            <converters:BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
            <converters:NullToBoolConverter x:Key="NullToBoolConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
            <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
            <converters:CurrencyConverter x:Key="CurrencyConverter"/>
            <converters:ImagePathConverter x:Key="ImagePathConverter"/>

            <!-- قوالب البيانات - DataTemplates -->
            <DataTemplate DataType="{x:Type viewmodels:DashboardViewModel}">
                <views:DashboardView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:EmployeesViewModel}">
                <views:EmployeesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:AttendanceViewModel}">
                <views:AttendanceView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:LeavesViewModel}">
                <views:LeavesView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:PayrollViewModel}">
                <views:PayrollView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:DepartmentsViewModel}">
                <views:DepartmentsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewmodels:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>

            <!-- الخط العربي - Arabic Font -->
            <FontFamily x:Key="ArabicFont">Segoe UI, Tahoma, Arial</FontFamily>

            <!-- الألوان الأساسية - Primary Colors -->
            <SolidColorBrush x:Key="PrimaryBrush" Color="#1976D2"/>
            <SolidColorBrush x:Key="PrimaryDarkBrush" Color="#0D47A1"/>
            <SolidColorBrush x:Key="AccentBrush" Color="#00BCD4"/>
            <SolidColorBrush x:Key="SuccessBrush" Color="#4CAF50"/>
            <SolidColorBrush x:Key="WarningBrush" Color="#FF9800"/>
            <SolidColorBrush x:Key="ErrorBrush" Color="#F44336"/>
            <SolidColorBrush x:Key="BackgroundBrush" Color="#F5F5F5"/>
            <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>

            <!-- أنماط النصوص - Text Styles -->
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="24"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </Style>

            <Style x:Key="SubHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="18"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryHueMidBrush}"/>
            </Style>

            <Style x:Key="BodyTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
            </Style>

            <!-- نمط البطاقة - Card Style -->
            <Style x:Key="CardStyle" TargetType="Border">
                <Setter Property="Background" Value="{DynamicResource MaterialDesignCardBackground}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Effect">
                    <Setter.Value>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.2"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- نمط الزر الأساسي - Primary Button Style -->
            <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="Height" Value="40"/>
            </Style>

            <!-- نمط حقل الإدخال - TextBox Style -->
            <Style x:Key="ArabicTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource MaterialDesignOutlinedTextBox}">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
                <Setter Property="Margin" Value="0,8"/>
            </Style>

            <!-- نمط القائمة المنسدلة - ComboBox Style -->
            <Style x:Key="ArabicComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource MaterialDesignOutlinedComboBox}">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
                <Setter Property="Margin" Value="0,8"/>
            </Style>

            <!-- نمط الجدول - DataGrid Style -->
            <Style x:Key="ArabicDataGridStyle" TargetType="DataGrid" BasedOn="{StaticResource MaterialDesignDataGrid}">
                <Setter Property="FontFamily" Value="{StaticResource ArabicFont}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FlowDirection" Value="RightToLeft"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="SelectionMode" Value="Single"/>
                <Setter Property="CanUserAddRows" Value="False"/>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>

