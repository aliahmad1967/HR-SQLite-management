<!-- =====================================================
     نظام إدارة الموارد البشرية - HR Management System
     PayrollView.xaml - شاشة الرواتب
     ===================================================== -->
<UserControl x:Class="HRManagementSystem.Views.PayrollView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان وأزرار الإجراءات -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="إدارة الرواتب"
                       Style="{StaticResource HeaderTextStyle}"/>

            <Button Grid.Column="1"
                    Content="توليد رواتب الشهر"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding GeneratePayrollCommand}">
                <Button.ContentTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Calculator" VerticalAlignment="Center"/>
                            <TextBlock Text="توليد رواتب الشهر" Margin="8,0,0,0"/>
                        </StackPanel>
                    </DataTemplate>
                </Button.ContentTemplate>
            </Button>
        </Grid>

        <!-- شريط الفلترة -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- السنة -->
                <ComboBox Grid.Column="0"
                          Style="{StaticResource ArabicComboBoxStyle}"
                          materialDesign:HintAssist.Hint="السنة"
                          ItemsSource="{Binding Years}"
                          SelectedItem="{Binding SelectedYear}"
                          Margin="0,0,16,0"/>

                <!-- الشهر -->
                <ComboBox Grid.Column="1"
                          Style="{StaticResource ArabicComboBoxStyle}"
                          materialDesign:HintAssist.Hint="الشهر"
                          ItemsSource="{Binding Months}"
                          DisplayMemberPath="Name"
                          SelectedItem="{Binding SelectedMonthItem}"/>
            </Grid>
        </Border>

        <!-- جدول الرواتب -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid Style="{StaticResource ArabicDataGridStyle}"
                      ItemsSource="{Binding PayrollList}"
                      SelectedItem="{Binding SelectedPayroll}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="الموظف" Binding="{Binding Employee.FullName}" Width="*"/>
                    <DataGridTextColumn Header="الراتب الأساسي" Binding="{Binding BasicSalary, StringFormat='{}{0:N0}'}" Width="120"/>
                    <DataGridTextColumn Header="البدلات" Binding="{Binding TotalAllowances, StringFormat='{}{0:N0}'}" Width="100"/>
                    <DataGridTextColumn Header="الخصومات" Binding="{Binding TotalDeductions, StringFormat='{}{0:N0}'}" Width="100"/>
                    <DataGridTextColumn Header="العمل الإضافي" Binding="{Binding OvertimeAmount, StringFormat='{}{0:N0}'}" Width="100"/>
                    <DataGridTextColumn Header="صافي الراتب" Binding="{Binding NetSalary, StringFormat='{}{0:N0}'}" Width="120"/>
                    <DataGridTextColumn Header="الحالة" Binding="{Binding Status}" Width="100"/>
                    <DataGridTemplateColumn Header="الإجراءات" Width="200">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.CalculatePayrollCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="حساب">
                                        <materialDesign:PackIcon Kind="Calculator" Foreground="{StaticResource PrimaryBrush}"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.ApprovePayrollCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="اعتماد">
                                        <materialDesign:PackIcon Kind="Check" Foreground="{StaticResource SuccessBrush}"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.ProcessPaymentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="صرف">
                                        <materialDesign:PackIcon Kind="CashMultiple" Foreground="{StaticResource AccentBrush}"/>
                                    </Button>
                                    <Button Style="{StaticResource MaterialDesignIconButton}"
                                            Command="{Binding DataContext.PrintPayslipCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding Id}"
                                            ToolTip="طباعة كشف الراتب">
                                        <materialDesign:PackIcon Kind="Printer" Foreground="Gray"/>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- مؤشر التحميل -->
        <ProgressBar Grid.Row="2"
                     IsIndeterminate="True"
                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>

