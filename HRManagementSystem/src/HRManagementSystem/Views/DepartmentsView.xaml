<!-- =====================================================
     نظام إدارة الموارد البشرية - HR Management System
     DepartmentsView.xaml - شاشة الأقسام
     ===================================================== -->
<UserControl x:Class="HRManagementSystem.Views.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             FlowDirection="RightToLeft">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- العنوان وأزرار الإجراءات -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="إدارة الأقسام"
                       Style="{StaticResource HeaderTextStyle}"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="إضافة قسم"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding AddDepartmentCommand}"
                        Margin="0,0,8,0">
                    <Button.ContentTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Plus" VerticalAlignment="Center"/>
                                <TextBlock Text="إضافة قسم" Margin="8,0,0,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </Button.ContentTemplate>
                </Button>

                <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                        Command="{Binding RefreshCommand}"
                        ToolTip="تحديث">
                    <materialDesign:PackIcon Kind="Refresh"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- المحتوى -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400" x:Name="EditPanel"/>
            </Grid.ColumnDefinitions>

            <!-- جدول الأقسام -->
            <Border Grid.Column="0" Style="{StaticResource CardStyle}" Padding="0" Margin="0,0,8,0">
                <DataGrid Style="{StaticResource ArabicDataGridStyle}"
                          ItemsSource="{Binding Departments}"
                          SelectedItem="{Binding SelectedDepartment}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="اسم القسم" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="الوصف" Binding="{Binding Description}" Width="200"/>
                        <DataGridTextColumn Header="المدير" Binding="{Binding Manager.FullName}" Width="150"/>
                        <DataGridTextColumn Header="عدد الموظفين" Binding="{Binding Employees.Count}" Width="100"/>
                        <DataGridCheckBoxColumn Header="نشط" Binding="{Binding IsActive}" Width="80"/>
                        <DataGridTemplateColumn Header="الإجراءات" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.EditDepartmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                ToolTip="تعديل">
                                            <materialDesign:PackIcon Kind="Pencil" Foreground="{StaticResource PrimaryBrush}"/>
                                        </Button>
                                        <Button Style="{StaticResource MaterialDesignIconButton}"
                                                Command="{Binding DataContext.DeleteDepartmentCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                ToolTip="حذف">
                                            <materialDesign:PackIcon Kind="Delete" Foreground="{StaticResource ErrorBrush}"/>
                                        </Button>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- نموذج التحرير -->
            <Border Grid.Column="1" 
                    Style="{StaticResource CardStyle}"
                    Visibility="{Binding IsEditing, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <TextBlock Text="بيانات القسم"
                               Style="{StaticResource SubHeaderTextStyle}"
                               Margin="0,0,0,16"/>

                    <TextBox Style="{StaticResource ArabicTextBoxStyle}"
                             materialDesign:HintAssist.Hint="اسم القسم"
                             Text="{Binding EditingDepartment.Name, UpdateSourceTrigger=PropertyChanged}"/>

                    <TextBox Style="{StaticResource ArabicTextBoxStyle}"
                             materialDesign:HintAssist.Hint="الوصف"
                             Text="{Binding EditingDepartment.Description, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap"
                             MinHeight="80"
                             AcceptsReturn="True"/>

                    <CheckBox Content="نشط"
                              IsChecked="{Binding EditingDepartment.IsActive}"
                              Margin="0,16,0,0"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,24,0,0">
                        <Button Content="حفظ"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding SaveDepartmentCommand}"
                                Margin="0,0,8,0"/>

                        <Button Content="إلغاء"
                                Style="{StaticResource MaterialDesignOutlinedButton}"
                                Command="{Binding CancelEditCommand}"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <!-- مؤشر التحميل -->
        <ProgressBar Grid.Row="1"
                     IsIndeterminate="True"
                     Style="{StaticResource MaterialDesignCircularProgressBar}"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>

